

最小堆的核心特性最小堆本质上是一棵完全二叉树（Complete Binary Tree），它满足两个关键性质：堆序性（Heap Property）：父节点的值总是小于或等于其子节点的值。这意味着，整个堆的根节点（最顶端）一定是所有元素中的最小值。结构性：它是一棵完全二叉树，除了最后一层外，每一层都是满的，且最后一层的节点都靠左排列。这使得我们可以用一个简单的数组来高效地存储它，而不需要复杂的指针。形象化理解：在数组存储中，如果父节点的索引是 $i$：其左子节点的索引是 $2i + 1$其右子节点的索引是 $2i + 2$其父节点的索引是 $(i-1) / 2$

时间复杂度（为什么它很快？）
最小堆之所以强大，是因为它在“插入”和“删除最小值”之间取得了完美的平衡：

查询最小值：$O(1)$（就在根部，一眼就能看到）。
插入新元素：$O(\log n)$。
删除最小值：$O(\log n)$。

排序数组
插入是 $O(n)$；
普通数组，找最小值是 $O(n)$。



当你遇到以下需求时，脑子里应该立刻蹦出“最小堆”：

1. 前 K 个最小元素：比如“在 10 亿个数里找最小的 100 个”。
2. 合并有序序列：比如“合并 5 个已经排好序的链表”。
3. 贪心算法：比如上面的亚马逊题目，每一步都要找当前消耗最小的任务。
4. 图论算法：比如 Dijkstra 最短路径算法或 Prim 最小生成树算法。
